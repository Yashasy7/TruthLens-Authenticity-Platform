package com.truthlens.backend.entity;

/**
 * Standardized named entity classification categories supported in TruthLens (Module 11).
 */
public enum ClaimEntityType {
    PERSON,
    ORG,
    LOCATION,
    DATE,
    MONEY,
    QUANTITY,
    EVENT,
    GENERAL
}
