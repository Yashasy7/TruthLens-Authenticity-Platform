package com.truthlens.backend;

import org.junit.jupiter.api.Test;

/**
 * Smoke test placeholder for TruthLens backend.
 *
 * <p>Full Spring context tests will be added in later stages once database
 * and security configuration is in place.</p>
 */
class TruthLensBackendApplicationTests {

    @Test
    void contextLoads() {
        // Intentionally empty — Spring context tests require database
        // configuration which is introduced in Stage 2.
    }
}
